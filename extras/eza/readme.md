# Conifer (eza)

## Installation

1. Copy the contents of `conifer.yml` into your eza theme file
   (at `$EZA_CONFIG_DIR`, or at `~/.config/eza/theme.yml` by default;
   you may need to create the directory if you haven't already).
